# =====================================================================
# Islamic Life Companion — Development dependencies
# Install with:  pip install -r requirements-dev.txt
# =====================================================================

# ---- Core (newer pins, same purpose as requirements.txt) ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
requests==2.32.3
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
email-validator==2.2.0
httpx==0.28.1
aiofiles==24.1.0

# ---- Testing ----
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# ---- Code quality ----
ruff==0.8.4
black==24.10.0
isort==5.13.2
mypy==1.13.0

# ---- API exploration (handy for hitting /docs locally) ----
# Nothing to add — Swagger UI comes with FastAPI.

# ---- Optional: a debugger ----
# pip install ipdb